# Specs

The current behavior of `goorouter`, organized by capability.

This directory is populated when an OpenSpec change is archived: the proposed deltas under `openspec/changes/<name>/specs/` are merged into this tree once the change is implemented and accepted.

Until the first change is archived, this directory is empty.
